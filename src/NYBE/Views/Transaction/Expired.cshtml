@{
    ViewData["Title"] = "Survey Expired";
}

<h2 class="text-center">@ViewData["Title"]</h2>

    <div class="row">
        <div class="col-sm-10 col-sm-offset-1">
            <hr />
            <h4 class="text-center">Whoops! Looks like you are too late to the party!</h4>
        </div>
    </div>

    @section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }
